## What and why

A short description of the change, in plain words, and the reason for it.

## Success criteria touched

List the IDs from `docs/SUCCESS-CRITERIA.md` this change touches (for example C3,
UI1). For each, say how the change keeps it holding.

## Evidence

- `npm test` summary line (tests, pass, fail):
- Lint: `npm run lint` clean (yes/no):
- Screenshot or recording, for anything visual:
- `npm pack --dry-run` size and file count, if packaging changed:

## Checklist

- [ ] Verbatim scripture only; no paraphrase or generated text.
- [ ] No untrusted text reaches a display sink (C3).
- [ ] Style matched by hand; no new dependency without a stated reason.
- [ ] Targets `main`.
